#!/usr/bin/env bash
# bin/detect <build-dir>

if [[ -f "$1/Aptfile" ]]; then
  echo "Apt"
  exit 0
else
  echo "'Aptfile' not found. Creating a default one..."
  echo -e "unixodbc\nunixodbc-dev" > "$1/new_directory/Aptfile"
fi
